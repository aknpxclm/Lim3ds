#ifndef MENUVALS_H_
#define MENUVALS_H_

enum Menu{
    StartMen = 0,
    MainMen,
    CombatMen
};

enum Rendering{
    GFX = 0,
    Combat
};

enum Msprites{
    Loading = 0,
    LobbyTop,
    LobbyBot
};

#define SCREEN_WIDTH  400
#define SCREEN_HEIGHT 240

#endif